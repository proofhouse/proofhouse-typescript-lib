# Runtime pins for a local checkout. .node-version no longer reaches
# mise, so Node is pinned here instead. CI declares its own matrix, and
# the packageManager field in package.json is what holds pnpm to one
# version everywhere.
[tools]
node = "24.18.1"
pnpm = "11.25.0"
