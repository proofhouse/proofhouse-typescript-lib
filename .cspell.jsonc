{
  "language": "en",
  // cspell ignores unknown keys silently, so this name must be exact:
  // without it, glob matching skips dotfiles and every dot-config in
  // this repo (.vale.ini, .yamllint.yaml, ...) escapes the gate.
  "enableGlobDot": true,
  "ignorePaths": [
    "**/*.d.ts",
    "**/*.log",
    ".claude/worktrees/**/*",
    // enableGlobDot lets the walk descend into .git, where loose objects
    // and the sample hooks flag dozens of words nobody wrote.
    ".git/**/*",
    ".rumdl_cache/**/*",
    ".stryker-tmp/**/*",
    ".vale/**/*",
    "coverage/**/*",
    "dist/**/*",
    "LICENSE",
    "node_modules/**/*",
    "pnpm-lock.yaml",
    "reports/**/*",
    "tmp/**/*"
  ],
  "dictionaryDefinitions": [
    {
      "name": "proofhouse",
      "path": "./.cspell-words.txt",
      "addWords": true
    }
  ],
  "dictionaries": ["proofhouse", "typescript", "node"]
}
